# Common Commands

### To clone specific branch from remote
When we have already cloned a repo (main branch) and want to get a specific remote branch locally:

### Fetch latest branches from remote
`git fetch origin`

### See all remote branches
`git branch -r`

### Switch to the required/specific branch

`git switch branch-name`

or 

`git checkout branch-name`